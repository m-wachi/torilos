###############################
### torilos02/work63    ###
###############################

torilos02/work62 + エディタ導入（4）

　　ファイルの読み込みの実装完了。
　　カーネル側のエディタモード不具合も一部修正。
　　これでエディタ実装はひとまずおわりっと。

　できてないこと／そのうちやりたいこと（今回から前回完了したものは消すことにする。）
　　c) 間接ブロックを持つinodeファイルのunlink

　　d) 引数渡しにおける領域オーバ等のエラー処理

　　e) writeシステムコールの実装 -> done.

	k) dupに対応できない。(writeシステムコール)
	
	l) 3段間接ブロックに対応したwrite
	
	m) readblockでフリーバッファを使いきったときのdirtyブロックの書き出しと
	　　読みかえ。	

	p) ディレクトリの対応 ->done. (48)
	
	q) mkfdimg_ext2の作りなおし
	
	r) nameiを複数ブロック読み込んで処理できるようにする。
	
	s) execveで失敗した際にもとに戻れるようにする処理
	
	t) timerのsleep処理にmutexを導入 -> done. (53)
	
	u) tflop_write()の特殊処理(オープンしてなくてもコンソール出力）を外す
	
	v) x87FPUのタスクスイッチ対応（優先度低）
	